# 1. Instalar Docker
sudo apt update
sudo apt install apt-transport-https ca-certificates curl software-properties-common -y
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io -y
sudo usermod -aG docker $USER
newgrp docker

Solución al error con Podman y nombres cortos de imágenes:

1. Crear el archivo para silenciar mensajes de Docker:
sudo touch /etc/containers/nodocker

2. Configurar Podman para usar Docker Hub como registro por defecto:
Edita el archivo de configuración de registros:

sudo nano /etc/containers/registries.conf
Agrega Docker Hub a unqualified-search-registries:

ini
[registries.search]
registries = ["docker.io", "quay.io"]

[registries.insecure]
registries = []

[registries.block]
registries = []

3. Ejecutar el comando de Podman usando el nombre completo de la imagen:
podman run -d 

# 2. Crear directorio y página HTML
mkdir -p /home/website
echo '<!DOCTYPE html>
<html>
<head>
  <title>Práctica Docker</title>
</head>
<body>
  <h1>Nombre: [Tu Nombre]</h1>
  <p>Matrícula: [Tu Matrícula]</p>
</body>
</html>' > /home/website/index.html

# 3. Ejecutar contenedor de NGINX
docker run -d --name my-nginx -p 8888:80 -v /home/website:/usr/share/nginx/html nginx:latest

# 4. Verificar en el navegador
curl http://127.0.0.1:8888
